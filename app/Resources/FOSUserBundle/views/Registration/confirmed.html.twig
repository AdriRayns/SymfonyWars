{% extends "FOSUserBundle::layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block fos_user_content %}

    <div class="row">
        <div class="col-xs-12 col-sm-12 col-lg-12 militarBody">
            <header class="colorMarron myFont myBigFont"><p> Confirmacion de registro</p></header>
            <h3 style="color: black;"> {{ 'registration.confirmed'|trans({'%username%': user.username}) }}</h3>
            {% if app.session is not empty %}
                {% set targetUrl = app.session.get('_security.' ~ app.security.token.providerKey ~ '.target_path') %}
                {% if targetUrl is not empty %}<p><a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a></p>{% endif %}
            {% endif %}
        </div>
    </div>


   
{% endblock fos_user_content %}
